const { response } = require("express");
const express = require("express");
const router = express.Router();
const { Op } = require("sequelize");
const { Posts, Users, sequelize, Likes } = require('../models')


class LikeRepository{
    all


}